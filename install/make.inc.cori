CC = cc
CXX = CC

CFLAGS = -fPIC -Wall -Wcheck

F90 = ftn
F90FLAGS = -r8 -warn all -fPIC

PYTHON = python

ARCH = ar cr

LD = $(CXX)
LDFLAGS =

LDD = $(CXX) --shared -fPIC

LIBS =

HDF5_LIBS = -L${HDF5_DIR}/lib -lhdf5 -lz
HDF5_INCLUDE = -I${HDF5_DIR}/include 

LAPACK = 
#-L${CRAY_LIBSCI_PREFIX_DIR}/lib -lsci_cray
